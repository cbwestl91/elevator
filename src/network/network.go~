package network

import(
	"fmt"
	"net"
)


func EstablishConnection(multicastAddr, multicastPort) {
	_, _ = net.ResolveUDPAddr("udp", multicastAddr:multicastPort)
	_, _ = net.ListenMulticastUDP("udp", nil, )

}

func MulticastImAlive() {

}
