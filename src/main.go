package main

import(
	"fmt"
	"fmt"
	"src/elevdriver.go"
)

func main(){
	//establish connection to UDP "I am alive" port

	//check if master exists

	//if master exists, run slave()

	//if no master, check if highest checksum, if so run master()	
}

func slave()

func master()

func local() //??
