package main

import(
	"fmt"
	"src/elevdriver.go"
)

func main(){
	//establish connection to UDP "I am alive" port

	//run slave(), check if master exists

	//

	//if no master, check if highest checksum, if so run master()	
}

func slave()

func master()

func local() //??
